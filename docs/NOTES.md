# AI Hub Playground Android NOTES

## 概要
Qualcomm AI Hub のモデル(視覚系+LLM)をタスクカテゴリ単位で動かせる Android アプリ。
モデルギャラリー → 選択 → カメラライブビューに結果オーバーレイ、という Ultralytics アプリ風 UX。
基準デバイスは Galaxy Z Fold 7。現在フェーズ: **Phase 2 完了(5カテゴリを実機で NPU 実行、モデルギャラリー+DL UX まで動作確認済み)**。

## 重要な決定
- 2026-07-11: **配布は当面 Google Play を使わず自己配布(サイトのDLリンク)。まず署名鍵を1本に固定**。`scripts/release.sh` を「通常実行では鍵を自動生成しない・無ければ中断／初回だけ `release.sh init-key` で生成」に変更(旧版は hostname 由来パスで毎回自動生成し、別マシンだと別鍵になって更新不能になる地雷だった)。**正規の署名鍵**: `release-keys/aihub-release.jks`(RSA 4096, alias=aihub, 2059年まで有効)、**SHA-256 = 3A:9F:8F:BB:30:80:7B:49:83:40:E5:EC:5B:34:91:2A:05:FD:97:4F:BC:BD:B6:95:7F:39:00:F7:34:FC:4B:22**。この鍵と同じ署名でしか上書き更新できないので `release-keys/` は要バックアップ(gitignore 済み=リポには入らない)。配布ホストは GitHub Releases を想定(無料CDN・帯域無料・版管理)、サイトはそこへ直リンク。更新のたび `versionCode` を +1 する
- 2026-07-09: **チャット/VLM のカタログは GGUF(llama.cpp, CPU/GPU)のみに確定。QAIRT(NPU)モデルは撤去**。理由: NPU の LLM/VLM は重みがスワップ不可の物理ピン留めメモリを要し、Fold7(12GB)でも再起動直後の空き ~6.4GB では 4B QAIRT のロードピークに届かず lmkd に SIGKILL(OOM)される。Samsung 標準+常駐の footprint が重く空きが伸びないのが根因で、当初疑った QNN 版数不整合は誤り。GGUF は版数フリーで実機生成を確認済みなので、実用体験は GGUF で提供する。最終カタログ: Multimodal=Qwen3-VL-4B/2B(GGUF)、Language=Qwen3-1.7B(GGUF)/Granite 4.0 micro(GGUF)、全 Apache-2.0
- 2026-07-08: **QNN HTP(NPU)実行には AndroidManifest に `<uses-native-library android:name="libcdsprpc.so" android:required="false"/>` が必須**。Android 12+ ではこれが無いと DSP RPC ライブラリを dlopen できず、`Failed to load skel / device_handle error=14001` で HTP 初期化が失敗し CPU に落ちる。宣言後は libQnnHtpV79Skel.so が cdsp ドメインにロードされ全ノードが NPU 実行される
- 2026-07-08: 実機接続は **Tailscale 経由の adb over TCP** で行う(`adb connect <tailscale-ip>:5555`)。WSL2 は USB 直結不可・interop 無効で Windows adb.exe も叩けないが、WSL 内に tailscale0 があり tailnet 直結できる。端末側で `adb tcpip 5555` 実行済み前提。初回接続時は端末で USB デバッグ承認が必要
- 2026-07-08: setup.sh は sudo 不要方式に変更 — システムに JDK17 が無ければ Temurin をリポ内 jdk17/ に取得、unzip が無ければ python3 で展開。WSL 等 sudo パスワード必須環境でも完結するため
- 2026-07-08: compileSdk 36 / targetSdk 34 — CameraX 1.6.1 が compileSdk 36 以上を要求するため(targetSdk は geniex-chat-android に合わせ 34 のまま)
- 2026-07-08: LiteRT は com.google.ai.edge.litert:litert:1.4.2(1.x 系 = org.tensorflow.lite API 互換)+ qnn-litert-delegate 2.48.0。AI Hub 配布の YOLOX tflite 自体が LiteRT 1.4.4 でビルドされており 1.x 系が正解
- 2026-07-08: モデルカタログは downloadUrl(S3 zip)+ zip 内パス方式 — qualcomm/* の HF リポにはモデル本体が無く、release_assets.json が指す S3 zip(precision/runtime 別)で配布されているため
- 2026-07-08: チャットアプリ(geniex-chat-android)とはリポジトリを分離 — 用途もランタイム構成も違うため。チャットは Phase 3 でこちらに統合予定
- 2026-07-08: 「AI Hub 全モデル対応」ではなく「タスクカテゴリ単位対応」を目標にする — 実装コストはモデル数でなくカテゴリ数(前処理・後処理)で決まるため
- 2026-07-08: モデルカタログは Apache-2.0 / MIT 系のみ。Ultralytics YOLO (AGPL) は不採用、検出は YOLOX を使う — Google Play 配布時の AGPL ソース開示義務を避けるため
- 2026-07-08: モデルは APK 同梱せず実行時に HuggingFace からダウンロード — ライセンス・APK サイズ両面で有利。geniex-chat-android の model_list.json 方式を拡張
- 2026-07-08: ビルド基盤は geniex-chat-android の scripts/ を移植 — クローン→setup.sh→build.sh で完結する可搬構成を踏襲
- 2026-07-08: フェーズ計画 — P1: カメラ+LiteRT/QNN+物体検出(YOLOX)で縦貫通 / P2: 分類・セグメンテーション・超解像・深度を横展開 / P3: GenieX チャット統合+カタログ UI / P4(任意): Whisper・Stable Diffusion

## 作業ログ
- 2026-07-11: **アプリ内アップデート通知(Play 非経由)を実装・実機確認**。`update/UpdateChecker.kt`(HttpURLConnection + kotlinx.serialization)が配布サーバの `latest.json`(versionCode/versionName/apkUrl/notes/minSupportedVersionCode)を取得し、インストール済み versionCode より新しければ GalleryActivity 起動時にダイアログで促す(「ダウンロード」で apkUrl をブラウザに ACTION_VIEW、`minSupportedVersionCode` 超で強制更新=後で不可)。参照先 URL は `UpdateChecker.LATEST_JSON_URL`(既定は GitHub raw の distribution/latest.json、配布先に差し替え)。`distribution/latest.json`(雛形)と `distribution/README.md`(リリース手順・署名鍵・サイト側注意)を追加。実機検証: WSL の一時HTTPサーバ(tailnet)に versionCode 999 の latest.json を置き URL を一時差し替え→起動時にダイアログ表示(notes 改行も反映)→「ダウンロード」で Chrome 起動を確認。テスト用の URL 差し替え・cleartext 許可は検証後に revert 済み
- 2026-07-11: **自己配布に向けて署名鍵を固定し release APK を実ビルド**。`release.sh init-key` で正規鍵を1回生成→`release.sh` で署名済み release APK を作成: `dist/aihub-playground-v0.1-1.apk`(123MB, debug 147MB より小)、apksigner verify で署名者 SHA-256 が正規鍵と一致。実機で debug をアンインストール→この署名済みAPKをインストール成功、署名スキームv4・署名者が debug 鍵(bb4b3b2c)から release 鍵(152b746d)に切替わったことを確認(起動確認は端末ロックで保留、コードは debug で全機能検証済み=同一コードの署名違い)。次: GitHub Releases へ配置+DLページ or アプリ内アップデート通知(latest.json)
- 2026-07-10: **撮影画面にタップフォーカス+フラッシュ切替を追加・実機確認**。CaptureActivity に (1) プレビュータップで `cameraControl.startFocusAndMetering`(AF/AE)を実行し合焦枠(黄色)をタップ位置に表示、(2) 右上にフラッシュ切替ボタン(OFF→ON→AUTO 循環、`ImageCapture.flashMode` に反映、アイコン追従、`hasFlashUnit()==false` の前面カメラでは無効化)。**ハマり2点**: (a) タッチリスナが ACTION_DOWN で false を返していて ACTION_UP が配信されずフォーカスが発火しなかった(DOWN で true を返して解消)。(b) 合焦枠 View が visibility=gone のうちは width/height=0 で中央寄せがずれる→dp から実サイズ算出して補正。実機で黄色枠がタップ位置に出ること、フラッシュが OFF/ON/AUTO でアイコン切替することを確認
- 2026-07-10: **チャット/VLM にアプリ内ライブ撮影を実装・実機でエンドツーエンド確認**。＋添付メニューの「カメラ」で端末カメラアプリではなく `CaptureActivity`(新規、CameraX Preview+ImageCapture の全画面プレビュー、シャッター/前面背面切替/閉じる)を起動→撮影 JPEG のパスを結果で返し、既存の 448px squareCrop→`savedImageFiles`→VLM 添付パイプラインに載せる。**ハマり2点**: (1) ＋ボタン(`btnAddImage`)は移植時に `openGallery()` 直呼びで、カメラ/写真の選択ポップアップ `showPopupMenu` が定義済みなのに未呼び出し→到達不能だった。＋を `showPopupMenu(it)` に繋ぎ直し。(2) `menu_layout.xml` の `btn_camera` に `android:visibility="gone"` が残っていて(geniex 側の名残)カメラ項目が非表示→削除して表示。実機検証(Qwen3-VL-2B GGUF/CPU): Gallery→2B→Load(CPU)→＋→カメラ→アプリ内ライブプレビュー→シャッター→チャットに撮影画像が添付され Send 有効化→送信すると VLM が撮影内容を正確に日本語描写("木製のテーブルの上にスマホが白いスタンド…黒いイヤホン")。CPU で TTFT 11.6s / デコード 29.74 tok/s。撮影→VLM の一体型 UX が成立
- 2026-07-09: **カタログを GGUF-only に確定・実機表示確認**。OOM で実用困難と判明した QAIRT モデル2種(Qwen2.5-VL-7B/qairt、Qwen3-4B-Instruct-2507/qairt)を model_list.json から撤去し、代わりに GGUF LLM を追加。最終 15 エントリ = 視覚系11 + Multimodal: Qwen3-VL-4B/2B-Instruct-GGUF(unsloth, vlm/llama_cpp) + Language: Qwen3-1.7B-GGUF(unsloth, chat/llama_cpp)/granite-4.0-micro-GGUF(ibm-granite, chat/llama_cpp)、全 Apache-2.0 / Q4_0。ビルド成功→実機インストール→ギャラリー3ドメイン(Vision緑/Multimodal紫/Language青)の表示を確認、GGUF 4種が正しく `→開く` でチャットへルーティングされる状態
- 2026-07-09: **Phase 3 着手: GenieX チャット+VLM を移植、ギャラリーを AI Hub 風 3ドメイン2階層に再編(UI配線まで実機確認、モデルDL+生成は未検証)**。(1) 依存: `com.qualcomm.qti:geniex-android:0.3.5`(Maven Central、ネイティブ/Rust モデルマネージャは AAR 同梱)+ markwon(Markdown)+ immersionbar。ndkVersion/extractNativeLibs/WAKE_LOCK/FileProvider を追加。**QNN .so 重複問題**: qnn-runtime(2.48, 視覚系で検証済み)と GenieX AAR(QNN 2.45 同梱)で libQnnHtp*.so 等7個が衝突→ `packaging.jniLibs.pickFirsts` で宣言順(qnn-runtime 先)により 2.48 採用。**NPU チャット(QAIRT)が 2.45 前提なら要再調整(未検証)**。(2) `AiHubApp`(Application)で `GenieXSdk.init`。(3) 移植: geniex `com.geniex.demo` → `com.aihub.playground.chat`(ChatActivity/ChatAdapter/GenerationConfigSample/bean.ModelData/utils/listeners)。R は別パッケージになったので明示 import 追加。レイアウト衝突は activity_main→activity_chat、item_model→item_chat_spinner にリネーム。drawable/color/font/Theme.AIHubChat を移植。(4) ギャラリー: `CatalogEntry` の downloadUrl/modelFile を nullable 化、TaskType.VLM・Runtime.QAIRT 追加。3ドメイン(Vision/Multimodal/Language)2階層アダプタに再構成、chat/vlm は ChatActivity へ、視覚系は従来どおり MainActivity へルーティング。LLM/VLM の取得済みは `ModelManagerWrapper.getPaths`(suspend)を onResume で非同期問い合わせしキャッシュ。(5) モデル: model_list.json に Qwen2.5-VL-7B(vlm/qairt/SM8750)・Qwen3-VL-4B/2B(vlm/llama_cpp)・Qwen3-4B-Instruct-2507(chat/qairt)を追加(全 Apache-2.0)。実機確認: ギャラリー3ドメイン表示、Qwen2.5-VL-7B タップで ChatActivity が当該モデル初期選択で起動、視覚系タップは従来どおりカメラ起動、GenieX 初期化クラッシュ無し。APK 101MB→153MB。ヘッダを "GenieX Chat"→"AI Hub ・ チャット" にリブランド。**ライブ検証(2026-07-09)**: (A) Qwen3-VL-2B(GGUF, llama.cpp)を DL→CPU で Load→テキスト生成成功(オンデバイスAIの利点を Markdown 整形で回答、Markwon 描画 OK)、画像添付は ACTION_PICK ピッカー起動まで確認。**バグ発見・修正**: llama.cpp の `nCtx=1024` が `maxTokens=2048` より小さく長い応答で「Context length exceeded」→ nCtx=4096 に拡張(chat/vlm 両方)。(B) Qwen3-4B-Instruct(NPU/qairt)。**2026-07-09 決着: NPU ロードのクラッシュ原因は QNN バージョンではなく OOM(メモリ不足)**。lmkd ログで確定(`min watermark is breached and swap is low ... signal 9 (Killed)`、アプリだけでなくランチャーまで巻き添え)。NPU の重みはスワップ不可の物理ピン留めメモリなので、Fold7(12GB)でも**再起動直後の空き ~6.4GB では 4B の QAIRT ロードのピークに届かず即 SIGKILL**。Samsung 標準+常駐アプリの footprint が重く空きが伸びない。**当初「QNN 2.45/2.48 版数不整合」と読んだのは誤り**(最初の 2.48 での落ちも全て OOM だった公算大)。ただし **QNN を 2.45 に統一したのは正しい構成**(GenieX 同梱が 2.45、混在は潜在バグ)なので維持。視覚系は 2.45 でも NPU 30fps を確認(回帰OK)。結論: **NPU の LLM/VLM(QAIRT)は本機の通常メモリ状態では実用困難**。チャット/VLM 体験は **GGUF(CPU/GPU, 版数フリー)で提供**が現実的(Qwen3-VL-2B は検証済みで動作)。qairt モデルはカタログに残すが OOM リスクありと明記すべき
- 2026-07-08: **Phase 2.3: 知覚系の仕上げ4点を実装・実機検証済み**。(1) **フロントカメラ切替** — 上部バーにカメラボタン、lensFacing トグルで再バインド。フロント時は PreviewView が左右反転表示するので OverlayView に `mirror` を追加し、検出ボックス/マスクの空間描画を左右反転してプレビューと一致させた(座標変換 mapX / drawMask で対応)。実機で自撮り時も 30fps 維持。(2) **検出しきい値 UI** — DetectionTask に Adjustable(score/IoU を @Volatile var 化)、下部に SeekBar 2本。score 0.95 で全ボックス消滅を確認。(3) **表示チューニング** — 分類は角丸パネル+確信度バー(top-1 をティール強調)に刷新(mouse 75% 等)、セグ凡例は名前+占有率%(swivel chair 46% 等)。(4) **超解像/深度/セグの領域選択(ROI)** — OverlayView に ScaleGestureDetector+ドラッグで動かせる正方 ROI(コーナーブラケット表示)を実装、frameRoi() でフレーム座標へ逆変換(mirror 対応)し RoiAware タスクへ毎フレーム供給。SuperRes は中央固定クロップ→任意領域クロップに、Seg/Depth は全画面伸長(歪み)→ROI 正方クロップ(アスペクト維持)に変更し、マスクは ROI 矩形内に描画。実機で ROI ドラッグ移動・ピンチ・超解像の bilinear/SR 比較が領域追従することを確認。ステータス(FPS)は下部インセットと重なるので右上へ移動
- 2026-07-08: **Phase 2.2: SegFormer 凡例に ADE20K ラベル名 + ギャラリーをカテゴリ別セクションUIに刷新(ビルド成功、実機検証は adb 再承認待ち)**。(1) `assets/labels/ade20k.txt`(150ラベル, index0=wall で背景ではない点に注意)を同梱、CatalogEntry に `labelsAsset` フィールド追加、VisionTask.create が zip にラベルの無いモデルは asset から読込。SegmentationTask は背景判定をインデックス基準からラベル名基準(name=="background"/"unlabeled")に変更し壁が透明化されない様に、凡例は面積降順 take(8)。(2) ギャラリーを RecyclerView 2ビュータイプ(セクションヘッダ+モデルカード)に再構成: カテゴリ別(物体検出🎯/画像分類🏷️/セグメンテーション🧩/超解像🔍/深度推定🌊)にアクセント色バー+件数、モデルカードは角丸22dp+押下で0.965縮小する stateListAnimator(触感)+ripple、左端にカテゴリのアクセント色縦バー+ステータスピル(絵文字アイコンは安っぽいとの指摘で撤去)。colors.xml(ダーク基調+カテゴリ5色)/bg_card_v2/bg_icon_tile/bg_status_pill/card_press(animator)/item_section_header/item_model_card を新規追加。ハマり: SegmentationTask の凡例 map で getOrElse のトレイリングラムダが `it` をシャドウしてコンパイルエラー→明示パラメータ `e ->` で解消
- 2026-07-08: **Phase 2.1: 超解像を bilinear vs SR 左右比較に + Transformer系/代替モデルを追加(全11モデル)、実機検証済み**。追加分の実測(全て QNN-HTP NPU): ViT(Transformer分類)30.4fps/20ms、SegFormer(Transformerセグ, ADE20K)24.7fps/41ms、**Depth-Anything V2(Transformer深度, float32)8.9fps/139ms** — float モデルも HTP に fp16 で載り CPU 落ちせず動作(MiDaS 17ms より重いが明瞭)、Real-ESRGAN x4(超解像GAN)25.9fps/36ms。超解像は同一クロップの bilinear ×4 と SR ×4 を左右比較インセット表示にして効果を可視化。汎用化2点: セグは出力 [1,H,W,C] ロジットの argmax に対応(DeepLabのクラスID直と両対応)、LiteRtEngine/各Taskは入力/出力 float32 に対応。UI自動検証は座標ずれ対策で uiautomator のテキスト指定タップに切替(部分一致で「モデルをダウンロード」を掴む罠→ボタンは完全一致で特定)。ライセンス注記: 物体検出は Apache が YOLOX のみ(他YOLOはAGPL)。ADE20K ラベル未同梱で SegFormer 凡例は classN 表示
- 2026-07-08: **Phase 2 完了。5カテゴリすべて Fold 7 実機で NPU 実行を確認**。VisionTask/VisionResult でタスク抽象化し、LiteRtEngine(QNN HTP/CPU 初期化 + 入力再量子化 LUT)を共通化。実測(全て QNN-HTP NPU): 物体検出 YOLOX 30fps/27ms、画像分類 MobileNet-v2 29.8fps/27ms(mouse/keyboard を正しく分類)、セグメンテーション DeepLabV3+ 28.7fps/18ms(tvmonitor をマスク、VOC21クラス)、超解像 XLSR 29fps/21ms(中央128px→512pxインセット)、深度 MiDaS 30fps/17ms(相対深度を Turbo カラーマップ)。モデルギャラリー(カタログ一覧・DL状態・同意ダイアログ・進捗バー)も動作。ハマり: MainActivity を exported=false にしたら adb 直接起動が Permission Denial → 検証は adb input tap でギャラリー操作を駆動(本来のユーザーフロー検証にもなった)。入力は全モデル io_type=image/value_range[0,1] なので入力テンソルの scale/zp で再量子化する汎用 LUT に統一(Midas は scale≠1/255)
- 2026-07-08: **Phase 1 縦貫通 完了。Galaxy Z Fold 7 実機で YOLOX が動作**。モデルDL(S3 zip→filesDir展開)→ LiteRT+QNN HTP delegate で全317ノードを Hexagon NPU(v79)実行 → letterbox前処理 → dequant → クラス別NMS → CameraX ライブビューにオーバーレイ描画。実測 **30fps / 推論27ms / QNN-HTP(NPU)**、cup/mouse/keyboard 等を正しく検出しボックス位置も一致。ハマりどころ2点: (1) `libcdsprpc.so` の uses-native-library 宣言漏れで HTP 初期化失敗→CPU落ち (2) 出力の scores/class_idx をスケール順で判定していたが class_idx のスケールが 0 のため取り違え→スコア常時0で検出ゼロ。テンソル名でマッピングし class_idx は生値使用で解決。画面ドーズ対策に FLAG_KEEP_SCREEN_ON 追加
- 2026-07-08: Phase 1 立ち上げ。scripts/ 移植(sudo 不要化改良込み)→ app/ Gradle 雛形(compileSdk 36)→ CameraX プレビュー+OverlayView+FPS 計測の骨組み → model_list.json(YOLOX INT8)→ **debug APK ビルド成功(100MB、QNN ネイティブ込み)**。YOLOX の実配布形式を調査: HF `qualcomm/Yolo-X` の release_assets.json → S3 zip(yolox-tflite-w8a8.zip = yolox.tflite 9.4MB + labels.txt + metadata.json)。モデル出力は box decode 済み(boxes[1,8400,4]/scores/class_idx)なので後処理は閾値+NMS のみで済む
- 2026-07-08: プロジェクト立ち上げ。geniex-chat-android 側の Claude Code セッションで企画・設計議論を実施し、CLAUDE.md / docs/DESIGN.md / docs/NOTES.md を作成。git init 済み(初回コミットは未実施)

## 次にやること
- [ ] (実機検証待ち) SegFormer 凡例に ADE20K 名が出るか / 新ギャラリーUIの表示・押下触感 — adb 再承認後に確認
- [x] フロントカメラ切替、検出の score/IoU 閾値 UI、分類/セグの表示チューニング(2026-07-08 実装・実機検証済み)
- [x] 超解像・深度・セグの ROI(ドラッグ+ピンチの領域選択)化。seg/depth の全画面伸長歪みも ROI 正方クロップで解消(2026-07-08 実装・実機検証済み)
- [ ] AI Hub 配布 zip(S3)のライセンス条項確認 — HF リポの license タグは "other"。配布アセット自体の利用条件を Qualcomm AI Hub の terms で要確認(YOLOX/MobileNet/DeepLab/XLSR=Apache-2.0, MiDaS=MIT で設定済みだが要裏取り)
- [ ] release_assets.json をランタイムで解決する方式の検討(バージョン更新への追従。現状はカタログに S3 URL 直書き)
- [x] Phase 3(移植): GenieX チャット+VLM を移植、ギャラリーを AI Hub 風3ドメインに再編、GGUF-only カタログで確定(2026-07-09、実機表示・GGUF 生成確認済み)
- [x] 撮影画像→VLM(Qwen3-VL GGUF)へ渡す「カメラで撮って質問」UX(アプリ内ライブ撮影 CaptureActivity、2026-07-10 実機確認済み)
- [ ] (任意)残タスク: Qwen3-VL-4B/Granite の DL→生成スモークテスト、撮影 UX にフラッシュ/タップフォーカス等の追加操作
- [~] Phase 3: GenieX チャット統合(geniex-chat-android から移植)、カタログに chat/vlm 型を追加。**VLM は geniex-chat-android に実装済み → 新規開発ではなく移植ベース**。あちらの `MainActivity.kt` に画像+テキスト→テキストの経路が丸ごとある: `VlmWrapper`(GenieX SDK)/画像添付(ACTION_PICK → `ImgUtil.squareCrop`/`downscaleAndSave`)/`VlmChatMessage`+`VlmContent("image"/"text")`/`applyChatTemplate`→`injectMediaPathsToConfig`→`generateStreamFlow`。llama.cpp と QAIRT(NPU) 両対応(`pluginId=="qairt"` で分岐)。model_list.json に `type:"vlm"` 済み: Qwen3-VL-2B/4B-Instruct・Qwen3.5-0.8B/2B(GGUF/llama.cpp)、**Qwen2.5-VL-7B-Instruct(qairt/NPU, ai-hub-models)**。ModelData.runtime("llama_cpp"|"qairt") でコンピュートユニット選択が変わる。Gemma-4 E2B/E4B は type:"chat" 止まり(llama.cpp の VLM 未配線、テキストのみ)。このアプリの「カメラで撮る→VLM に渡して質問」UX と自然に接続できる。2026-07-08: VLM 前倒しは見送り、知覚系の仕上げを先にする方針(B)を選択(仕上げは Phase 2.3 で完了)
- [ ] ai-hub-apps の前後処理コードと照合し流用した箇所は LICENSE/NOTICE 整備(現状は独自実装なので出自明記は未発生)
- [ ] モデル追加を容易にする(現状カタログ追記だけで済むが、出力テンソル名の想定が外れるモデルは個別対応が要る)
