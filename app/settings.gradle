pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

// ディレクトリ名に依らず APK 出力名を app-debug.apk に固定する
rootProject.name = 'app'
